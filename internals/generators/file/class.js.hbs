/**
 * {{ sentenceCase summary }}
 */

export class {{ properCase name }} {
  public x: number;
  constructor(x: number) {
    this.x = x;
  }

  /** Method description */
  public method(y: number): number {
    return this.x * y;
  }
}
